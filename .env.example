# Server-side only. Used by api/transcribe.ts (Vercel function). Never shipped to the client.
OPENAI_API_KEY=sk-...

# Optional: override the transcription model (must support word timestamps).
# OPENAI_TRANSCRIBE_MODEL=whisper-1
